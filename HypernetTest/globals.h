#pragma once

#include "Branch.h"
#include "Hypernet.h"

#define MAX_DIMENSIONAL 3

extern std::vector<Branche> Bin;
extern int num0, num2;
extern int chr, ch1, ch2, chrs, decomp1, decomp2, decomp3, bridges, pendunt, factors;
/*
extern vector<vector<bool>> maskApc;
extern vector<bool> maskMenc;
extern vector<bool> visitedNodes;*/

